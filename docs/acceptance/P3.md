# P3 app-host 回收台账

## rc.10 — K5 与 Router

- SOURCE：Node `v24.13.1`，`npm 11.8.0`；`npm run check` 为 73 tests / 71 PASS / 0 FAIL / 2 `REAL_BROWSER` SKIP；7/7 源码 mutation 检出；`npm run test:types` PASS。
- REAL_HOST：隔离 `DSH_HOME` 的真实 DSH web profile 已加载包根插件，配置证据见 `p3-20260919/stage1/`。
- REAL_UI：真实 Chrome 的 Settings 出现「供应商」段与来源状态表；没有执行订阅登录，也没有启用 Coding OAuth Gateway。
- BLOCKED：真实订阅会话需要用户账户动作并可能消耗 Codex 配额；未获数量授权，因此不声称 Router 能调用真实订阅模型。
- SAFETY：没有触碰 `~/.dsh`，没有使用 3080；真实门端口为 39110，进程已停止。

当前最多为 `PARTIAL`，不是用户 `ACCEPTED`。

## rc.11 — 多源应用库

- SOURCE：全仓 78 tests / 76 PASS / 0 FAIL / 2 `REAL_BROWSER` SKIP；AH-L01–L06 为 5/5；7/7 mutation 检出；严格类型检查 PASS。
- PACKAGE：精确 peer `@hanamesh/lib-provision@0.1.0-rc.1`，vendor SHA-256 `386d57361ccc3c8578f54cad8137acc291dd72e01b3ff987e65dfaa716f2e0b0`；包内存在 `dist/provision/index.js` 与两份 schema。
- REAL_HOST：全新隔离 `DSH_HOME=/private/tmp/hm-p3-rc11-iX5gID` 的 web profile 通过 bundle 安装并加载 rc.11；端口 39111，未使用 3080。
- REAL_UI：当前 session Browser 显示侧栏「应用库」；fixture 三项分别为 Vibe `安装`、repository-only `只可浏览`、普通插件 `只可浏览`。首轮发现白底白字后修复并重复安装/复验；最终截图为 `p3-20260919/stage2/library-list.png`。
- NOT_RUN：没有点击 Vibe 安装；rc.11 fixture 尚无可安装的真实 Vibe npm 包/runtime，因此“安装→重启→打开”留给 rc.12 阶段 3。

rc.11 最高仍为 `PARTIAL`，不是用户 `ACCEPTED`。

## rc.12 — runtime 描述符与应用包规范

- SOURCE：86 tests / 84 PASS / 0 FAIL / 2 `REAL_BROWSER` SKIP；8/8 源码 mutation 检出；严格类型检查 PASS。
- CONTRACT：owned deployment 可用互斥的 `runtime: {manifest,item,exec}` 代替 `command`；路径、lib-provision 账本版本与 X_OK 均失败关闭为 `INVALID_RUNTIME` / `RUNTIME_MISSING`。
- PACKAGE：新增 `docs/APP_PACKAGE.md`，固定应用 bundle 的五文件形状、精确 app-host peer、无 runtime dependencies 与不可覆盖版本规则。
- RELIABILITY：真实 SIGKILL 测试发现并修复 guardian 日志管道 `EPIPE` 可早于进程组清理的竞态；10 次连续 crash 回归及全套 X02/X03 均通过。
- ARTIFACT：`hanamesh-dsh-app-host-0.1.0-rc.12.tgz`，SHA-256 `f8417c65805488df4412156e1d8566c46076bee736dbe2973a937a2ac8515ae6`。
- NOT_RUN：Vibe runtime 的安装、启动和 session Browser iframe 仍待 Vibe rc.9 产物完成后执行。

rc.12 当前最高仍为 `PARTIAL`，不是用户 `ACCEPTED`。

## Vibe rc.9 — 应用包 → 接管 → 供给 → 打开

- PACKAGE：`@hanamesh/app-vibe-trading@0.1.0-rc.9`，npm 包 11,648 bytes；五个声明文件加 npm 固有 `package.json`，无 runtime dependencies，精确 peer app-host rc.12。
- REAL_RUNTIME：darwin-arm64 归档 SHA-256 `7145a0c4122f6c95da0e4279a5988c9acd4618e52d4c63e6e1405f156b5dc0cf`；lib-provision 的 `PATH=''` verify、账本、X_OK 与真实 app-host open 均通过；独立门 79.1 秒，首页 200、close 后 stopped。
- REAL_HOST：隔离 `DSH_HOME=/private/tmp/hm-p3-rc12-vibe.cR3rqF`，端口 39112，未用 3080；本地 tgz bundle 安装后，应用库先显示「补齐运行时」，供给后显示「打开」。
- REAL_UI：只用当前 session Browser；iframe 显示原版 Vibe 0.1.15 首页和导航，输入框未发送，未使用 provider/Codex 额度。
- DIFFERENCE：第一次 DSH 启动漏 `--no-open`，DSH 自动向默认浏览器发出一次打开请求；没有公共浏览器操作，立即停止并改为 `--no-open`，此后所有交互都在 session Browser。该偏差不记为公共浏览器验收。
- BLOCKED：coding-oauth 登录/网关/真实回复需用户账户与额度数量授权。
- NOT_RUN：公开 npm / GitHub Release、远端 Community Market、两种桌面壳、tether、core 套件互斥。

P3 当前上限为 **🧪 DELIVERED / PARTIAL**；不写 ACCEPTED。

## rc.13 — 上架前提补齐（本机回收 2026-09-19，代码与 rc.12 相同）

- 判定：rc.12 缺 P3 ⑥「上架前提」（`license: MIT` + 仓根 `LICENSE` + `repository` 字段），且 DSH peer 用了 `^` 范围（`^4.0.2` / `^0.1.5-alpha.1` / `^3.18.2`），与 core/usage 的精确钉不一致（DELIVERY_RULES §1.6）。
- 补齐：`package.json` `license: MIT`、`repository` → `github.com/yzsnstotz/hanamesh-plugin-app-host`、peer 精确 `4.0.2` / `0.1.5-alpha.1` / `3.18.2`；新增 `LICENSE`（MIT，HanaMesh）并进 `files`；README 顶部加「收录不代表审核或推荐」与双向互斥说明。`src/`、`profile/`、`schemas/` 零改动。
- SOURCE：`npm run check` = build + X01 + 86 tests（84 PASS / 0 FAIL / 2 `REAL_BROWSER` SKIP）+ 8/8 mutation 检出；`npm run test:types` PASS。
- ARTIFACT：`hanamesh-dsh-app-host-0.1.0-rc.13.tgz`，SHA-256 `f730ea20faff613913eb9952a02a72b831613356386d8c30771321d8f5f1619d`；包内含 `LICENSE`、`profile/cordis.patch.yml`。
- NOT_RUN：rc.12 的 REAL_HOST/REAL_UI 证据不重跑（代码字节相同，只改元数据）；O3 用 rc.13 tgz 做套件门。

rc.13 最高仍为 🧪，不是用户 `ACCEPTED`。

## rc.14 — 去耦：删除客户端对 `hanameshCore` 的死读取（本机耦合审计 2026-09-20）

- 判定：`src/client-ui.js` 在浏览器侧 `apply(ctx)` 里 `ctx.get?.('hanameshCore')`。钉版本 DSH 的浏览器 root context 由 `dsh-client-modules` 单独创建（`dsh-app-boot` 的宿主 ctx 不跨到浏览器），宿主服务 `hanameshCore` 永远不在其中：该读取恒为 null，顶栏文案恒显「未安装 HanaMesh Core」——对已装 Core 的用户是误导，且 AH-L08 门在此实现下无法区分有/无 Core。属设计错位（P3 路线第 174 条把宿主服务读取写到了页面侧）。
- 补齐：删除 `coreService`/`account` 状态与顶栏 `small`；本包对 Core 现在零引用（`grep -rn hanameshCore src/` 只剩说明注释）。Core 状态由 Core 自己的设置段展示（P1 阶段 3）。P3 路线文档第 174 条、AH-L08、A5-2 同步改写。
- SOURCE：`npm run check` = build + X01 + 86 tests（84 PASS / 0 FAIL / 2 `REAL_BROWSER` SKIP）+ 8/8 mutation；`test:types` PASS。
- ARTIFACT：`hanamesh-dsh-app-host-0.1.0-rc.14.tgz`，SHA-256 `169c63d812efea0c1b64a6527d52c64b5f454046b18a7f4cb14467e390d7304c`；包内 `dist/client-ui.js` 无 `hanameshCore` 调用。
- NOT_RUN：REAL_UI 不重跑（只删了一段文案与一次恒 null 的读取）。

rc.14 最高仍为 🧪，不是用户 `ACCEPTED`。

## rc.15 — 线上目录源真实门暴露的校验缺口（本机 2026-09-20）

- 判定：O2 部署后把 `https://market.hanamesh.com/catalog-source.json` 加进用户桌面的应用库来源，`GET /hanamesh/library` 报 `INVALID_CATALOG_PAGE`。原因：`validateItem` 的允许键集漏了共享 schema（`schemas/catalog-provider-page.schema.json`）允许的 `updatedAt`，线上每条都带它 → 12,121 条全部被拒。P3 只对 fixture 验过，O2 只对 schema 副本验过，两边没互相打过。
- 补齐：允许键集按 schema 补 `updatedAt`（RFC3339 校验）；新增 `tests/library-catalog-live-shape.test.mjs`（AH-L09）。
- SOURCE：87 tests（85 PASS / 2 REAL_BROWSER SKIP）、8/8 突变、类型过。tgz `hanamesh-dsh-app-host-0.1.0-rc.15.tgz` SHA-256 `0ccac049cb838423dbaad001b4bafadcdea906e00f259dbbcc2b74cc33665c3d`。
- REAL_NET：见下（用户桌面 + 线上源）。

## rc.16 — 应用库对真实目录的可用性（本机 2026-09-20）

- 判定：rc.15 的 `list()` 不接 `q`/`cursor`、不过滤类别，对 12,121 条真实目录只显示前 50 条，`@hanamesh/app-vibe-trading` 不可达；客户端无搜索/翻页。
- 补齐：目录请求默认 `category=hanamesh-app`（站点过滤已验证只返回 Vibe），`q`/`cursor` 透传，路由白名单三参数，客户端搜索框 + 「只看应用」+「更多」。`tests/library-query.test.mjs`（AH-L10）。
- SOURCE：88 tests（86 PASS / 2 SKIP）、8/8 突变、类型过。tgz `hanamesh-dsh-app-host-0.1.0-rc.16.tgz` SHA-256 见 `.sha256`。
- REAL_NET：用户桌面 + `https://market.hanamesh.com/catalog-source.json` → 应用库默认列出 1 条应用（Vibe，可安装）；见下条记录。
